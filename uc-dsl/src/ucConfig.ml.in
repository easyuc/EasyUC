(* source of UcConfig module *)

(* configuration module: build script substitutes for ECtheories the
   fully qualified pathname of EasyCrypt theories directory *)

let ec_theories_dir = "ECtheories"
