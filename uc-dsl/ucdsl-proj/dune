(dirs src 3rdparty libs)

(env
 (_ (flags -rectypes -warn-error -a+31 -w +28+33-9-23-32-58-67-69)
    (ocamlopt_flags -O3 -unbox-closures)))

(install
 (section bin)
 (files (src/ucdsl.exe as ucdsl)
        (src/ucEasyCryptCommentMacrosTest.exe as easycrypt-comment-macros-test)))
