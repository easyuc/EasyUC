load RW.

functionality Real.

var pt : port.
assumption H : envport func adv pt.
var xs : int list.

real.

send pt@RW.DIR.D.req(xs)$func.
step.

hint /plus_size_le_hints.

step.

step.
(* assertion fails, as expected simplification didn't happen *)
assert msg_out 
((func, 1))@RW.DIR.D.rsp(1 :: 2 :: xs)@pt
ctrl_env.
