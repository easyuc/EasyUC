load Debug.

functionality Real.

var pt1 : port.
var pt2 : port.
assumption H1 : envport func adv pt1.
assumption H2 : envport func adv pt2.
var x : EFI_GUID.

real.

send pt1@Debug.DIR.D.req(pt2)$func.
step.
step.
step.
step.
assert msg_out 
((func, 1))@Debug.ADV.D.req@((adv, 2))
ctrl_adv.

send ((adv, 2))@Debug.ADV.D.rsp$func.
debug.
prover timeout=5.
step.

